#include <stdio.h>
/**
 *main - Prints the method
 *Return: Always 0
 */
int main(void)
{
int i, m;
for (i = 'a' ; i <= 'z' ; i++)
putchar(i);
for (m = 'A' ; m <= 'Z' ; m++)
putchar(m);
putchar('\n');
return (0);
}
